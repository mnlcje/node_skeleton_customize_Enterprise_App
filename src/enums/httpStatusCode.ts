export enum HttpStatusCode {
    OK = 200,
    BAD_REQUEST = 400,
    UNAUTHORIZED = 401,
    FORBIDDEN = 403,
    NOT_FOUND = 404,
    UNSUPPORTED_ACTION = 405,
    VALIDATION_FAILED = 422,
    SERVER_ERROR = 500,
    SERVER_UNAVAILABLE = 503,
    CREATED = 201
}
